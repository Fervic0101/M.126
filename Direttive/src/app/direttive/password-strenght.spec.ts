import { PasswordStrenght } from './password-strenght';

describe('PasswordStrenght', () => {
  it('should create an instance', () => {
    const directive = new PasswordStrenght();
    expect(directive).toBeTruthy();
  });
});
