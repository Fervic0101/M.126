import { Mydirettive } from './mydirettive';

describe('Mydirettive', () => {
  it('should create an instance', () => {
    const directive = new Mydirettive();
    expect(directive).toBeTruthy();
  });
});
