import { ColorNumber } from './color-number';

describe('ColorNumber', () => {
  it('should create an instance', () => {
    const directive = new ColorNumber();
    expect(directive).toBeTruthy();
  });
});
