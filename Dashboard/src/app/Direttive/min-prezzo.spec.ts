import { MinPrezzo } from './min-prezzo';

describe('MinPrezzo', () => {
  it('should create an instance', () => {
    const directive = new MinPrezzo();
    expect(directive).toBeTruthy();
  });
});
